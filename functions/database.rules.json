{
  "rules": {
    "rooms": {
      ".read": "auth != null"
    },
    "messages": {
      ".write": "auth != null",
      ".read": "auth != null"
    },
    "music": {
      ".read": "auth != null",
      "$room_id": {
        "nowplaying": {
          "$track_id": {
            ".write": "auth != null && ((data.exists() && !newData.exists() && data.child('endTime').val() <= now / 1000) || (data.exists() && newData.hasChildren(['rescrape']) && newData.child('rescrape').val() === true))"
          }
        }
      }
    },
    "status": {
      ".read": "auth.uid != null",
      "$room_id": {
        "$uid": {
          ".write": "auth.uid == $uid"
        }
      }
    }
  }
}
